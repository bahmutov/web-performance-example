{
  "name": "web-performance-example",
  "version": "1.0.0",
  "description": "Playing with web performance metrics",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serve public",
    "perf": "lighthouse http://localhost:3000 --only-categories=performance --no-enable-error-reporting",
    "measure": "start-test 3000 perf"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lighthouse": "^10.3.0",
    "serve": "^14.2.0",
    "start-server-and-test": "^2.0.0"
  }
}
